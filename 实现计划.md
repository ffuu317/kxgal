# 《食证圈》实现计划

## 1. 实施目标

第一版先实现最基础的内容社区能力，让用户可以完成登录、浏览、上传、评论等核心操作。复杂的指纹绑定、AI 验真、信用币、悬赏、商家认证、管理后台等能力后续再逐步添加。

第一版核心闭环：

```text
用户注册登录 -> 浏览首页内容 -> 查看详情 -> 上传帖子 -> 评论互动 -> 查看个人内容
```

## 2. 第一版功能范围

## 2.1 必须实现

| 模块 | 功能 |
|---|---|
| 用户 | 注册、登录、退出登录、获取当前用户 |
| 首页 | 帖子列表、帖子卡片、基础搜索 |
| 帖子 | 上传图片、发布帖子、查看帖子详情 |
| 评论 | 发表评论、查看评论、删除自己的评论 |
| 点赞 | 点赞帖子、取消点赞 |
| 我的 | 查看个人资料、查看自己发布的帖子 |
| 文件上传 | 上传头像、上传帖子图片 |

## 2.2 暂不实现

| 模块 | 延后原因 |
|---|---|
| 指纹绑定 | 涉及生物识别、加密存储、合规流程，后续单独设计 |
| 身份证实名 | 涉及敏感信息和人工审核，后续实现 |
| 信用币 | 第一版先不做经济系统，避免业务复杂度过高 |
| 等级系统 | 依赖信用币和可信行为数据，后续添加 |
| AI 验真 | 第一版先预留审核状态字段，后续接入 AI 服务 |
| 悬赏 | 依赖地图、定位、奖励结算和 AI 图片审核，后续实现 |
| 商家认证 | 依赖资质审核和商家主页，后续实现 |
| 商家圈 | 依赖商家体系和悬赏记录，后续实现 |
| 管理后台 | 第一版可先通过数据库或简单脚本处理异常数据 |

## 3. 推荐技术架构

项目仍按前端、后端和后续 AI 服务拆分，但第一版只需要实现前端和后端。

```text
eattruth
├── backend/                 # Node.js 后端服务
├── mobile/                  # React Native 移动端
├── ai-service/              # 后续 AI 服务，第一版可先不实现
└── docker-compose.yml       # 本地开发编排
```

建议技术选型：

| 层级 | 推荐方案 | 第一版用途 |
|---|---|---|
| 移动端 | React Native | 登录、首页、帖子、评论、我的 |
| 后端 | Node.js + NestJS 或 Express | 用户、帖子、评论、点赞、上传 |
| ORM | Prisma | 管理数据模型和迁移 |
| 数据库 | PostgreSQL | 存储用户、帖子、评论、点赞 |
| 文件存储 | 本地存储或对象存储 | 存储头像和帖子图片 |
| 缓存 | Redis，可选 | 第一版不是必须，可后续加入 |

## 4. 第一版数据模型

第一版只保留最基础的数据表。

## 4.1 用户表 `users`

核心字段：

1. `id`：用户 ID。
2. `phone`：手机号。
3. `passwordHash`：密码哈希。
4. `nickname`：昵称。
5. `avatarUrl`：头像地址。
6. `bio`：个人简介。
7. `status`：账号状态，默认正常。
8. `createdAt`：创建时间。
9. `updatedAt`：更新时间。

## 4.2 帖子表 `posts`

核心字段：

1. `id`：帖子 ID。
2. `authorId`：发布者 ID。
3. `title`：标题。
4. `content`：正文。
5. `coverImageUrl`：封面图。
6. `imageUrls`：图片列表。
7. `merchantName`：关联商家名称，可选。
8. `tags`：标签列表。
9. `likeCount`：点赞数。
10. `commentCount`：评论数。
11. `status`：帖子状态。
12. `createdAt`：发布时间。
13. `updatedAt`：更新时间。

## 4.3 评论表 `comments`

核心字段：

1. `id`：评论 ID。
2. `postId`：帖子 ID。
3. `authorId`：评论用户 ID。
4. `content`：评论内容。
5. `status`：评论状态。
6. `createdAt`：评论时间。
7. `updatedAt`：更新时间。

## 4.4 点赞表 `post_likes`

核心字段：

1. `id`：点赞 ID。
2. `postId`：帖子 ID。
3. `userId`：用户 ID。
4. `createdAt`：点赞时间。

约束：

1. 同一用户对同一帖子只能点赞一次。
2. 点赞和取消点赞需要同步更新帖子点赞数。

## 4.5 文件表 `files`

核心字段：

1. `id`：文件 ID。
2. `ownerId`：上传用户 ID。
3. `url`：访问地址。
4. `mimeType`：文件类型。
5. `size`：文件大小。
6. `purpose`：用途，例如头像、帖子图片。
7. `createdAt`：上传时间。

## 5. 阶段划分

## 5.1 第一阶段：工程初始化

目标：搭建可以运行的前后端工程。

主要任务：

1. 创建 `backend/` 和 `mobile/` 目录。
2. 初始化 Node.js 后端项目。
3. 初始化 React Native 移动端项目。
4. 配置数据库连接和环境变量。
5. 配置 Prisma schema 和迁移命令。
6. 增加基础启动脚本。
7. 增加基础健康检查接口。

交付物：

1. 后端服务可以启动。
2. 移动端项目可以启动。
3. 数据库可以连接。
4. 数据库迁移可以执行。

## 5.2 第二阶段：用户注册登录

目标：完成用户身份体系。

主要任务：

1. 实现手机号注册。
2. 实现密码登录。
3. 实现退出登录。
4. 实现 JWT 鉴权。
5. 实现获取当前用户接口。
6. 实现修改昵称、头像、简介。
7. 移动端完成登录页、注册页、个人资料页。

接口规划：

```text
POST /auth/register
POST /auth/login
POST /auth/logout
GET  /users/me
PATCH /users/me
POST /files/avatar
```

交付物：

1. 用户可以注册账号。
2. 用户可以登录并保持会话。
3. 用户可以查看和修改个人资料。
4. 未登录用户不能发布帖子和评论。

## 5.3 第三阶段：首页浏览

目标：完成内容浏览主路径。

主要任务：

1. 实现帖子列表接口。
2. 实现帖子详情接口。
3. 实现基础搜索，支持按标题、正文、商家名称、标签查询。
4. 移动端实现首页瀑布流或方块帖子流。
5. 移动端实现帖子详情页。
6. 帖子卡片展示封面、标题、作者、点赞数、评论数。

接口规划：

```text
GET /posts
GET /posts/:id
GET /posts/search
```

交付物：

1. 用户打开 App 后可以看到首页帖子流。
2. 用户可以进入帖子详情。
3. 用户可以搜索帖子、商家或标签。

## 5.4 第四阶段：帖子上传与发布

目标：用户可以发布美食内容。

主要任务：

1. 实现帖子图片上传。
2. 实现发布帖子接口。
3. 实现编辑自己的帖子。
4. 实现删除自己的帖子。
5. 移动端实现发布页。
6. 发布表单包含标题、正文、图片、商家名称、标签。
7. 对上传文件做基础校验，包括类型和大小。

接口规划：

```text
POST   /files/post-image
POST   /posts
PATCH  /posts/:id
DELETE /posts/:id
```

交付物：

1. 登录用户可以上传图片。
2. 登录用户可以发布帖子。
3. 用户只能编辑和删除自己的帖子。
4. 新发布帖子可以出现在首页。

## 5.5 第五阶段：评论互动

目标：完成基础社区互动。

主要任务：

1. 实现评论列表接口。
2. 实现发表评论接口。
3. 实现删除自己的评论。
4. 发布评论后更新帖子评论数。
5. 移动端实现评论区。
6. 评论区按时间倒序或正序展示。

接口规划：

```text
GET    /posts/:id/comments
POST   /posts/:id/comments
DELETE /comments/:id
```

交付物：

1. 用户可以查看帖子评论。
2. 登录用户可以发表评论。
3. 用户可以删除自己的评论。
4. 帖子评论数正确更新。

## 5.6 第六阶段：点赞功能

目标：补齐最基础的互动反馈。

主要任务：

1. 实现点赞帖子。
2. 实现取消点赞。
3. 查询帖子列表时返回当前用户是否已点赞。
4. 查询帖子详情时返回当前用户是否已点赞。
5. 移动端实现点赞按钮状态。

接口规划：

```text
POST   /posts/:id/like
DELETE /posts/:id/like
```

交付物：

1. 用户可以点赞帖子。
2. 用户可以取消点赞。
3. 点赞数展示正确。
4. 同一用户不能重复点赞同一帖子。

## 5.7 第七阶段：“我的”页面

目标：用户可以查看自己的内容和基础资料。

主要任务：

1. 实现我的帖子列表。
2. 实现我的评论列表。
3. 实现个人资料展示。
4. 移动端实现“我的”页面。
5. “我的”页面展示头像、昵称、简介、发帖数。

接口规划：

```text
GET /users/me/posts
GET /users/me/comments
```

交付物：

1. 用户可以查看自己发布的帖子。
2. 用户可以查看自己发表的评论。
3. 用户可以从“我的”页面进入个人资料编辑。

## 6. 第一版页面规划

## 6.1 登录页

包含：

1. 手机号输入。
2. 密码输入。
3. 登录按钮。
4. 注册入口。

## 6.2 注册页

包含：

1. 手机号输入。
2. 密码输入。
3. 昵称输入。
4. 注册按钮。

## 6.3 首页

包含：

1. 顶部搜索框。
2. 帖子卡片列表。
3. 底部导航栏。
4. 发布入口。

## 6.4 帖子详情页

包含：

1. 图片展示。
2. 标题。
3. 正文。
4. 作者信息。
5. 点赞按钮。
6. 评论列表。
7. 评论输入框。

## 6.5 发布页

包含：

1. 图片选择。
2. 标题输入。
3. 正文输入。
4. 商家名称输入。
5. 标签输入。
6. 发布按钮。

## 6.6 我的页面

包含：

1. 头像。
2. 昵称。
3. 简介。
4. 我的帖子。
5. 我的评论。
6. 退出登录。

## 7. 后续迭代规划

## 7.1 第二版：基础审核与举报

主要功能：

1. 举报帖子。
2. 举报评论。
3. 简单管理后台。
4. 帖子隐藏和恢复。
5. 评论隐藏和恢复。
6. 基础敏感词过滤。

## 7.2 第三版：商家基础能力

主要功能：

1. 商家信息页。
2. 帖子关联商家。
3. 商家搜索。
4. 商家下的帖子和评论聚合。
5. 商家认领申请。

## 7.3 第四版：信用币与等级

主要功能：

1. 信用币余额。
2. 信用币交易明细。
3. 每日登录奖励。
4. 评论消耗信用币。
5. 用户等级展示。
6. 违规扣币。

## 7.4 第五版：AI 验真

主要功能：

1. 图片真实性检测。
2. 评论水军检测。
3. 重复图片检测。
4. AI 审核状态展示。
5. 人工复核入口。

## 7.5 第六版：悬赏模块

主要功能：

1. 发布悬赏。
2. 接收悬赏。
3. 上传验证图片。
4. AI 验真。
5. 奖励信用币。
6. 悬赏记录同步到商家页。

## 7.6 第七版：指纹与实名

主要功能：

1. 指纹绑定。
2. 指纹重复率检测。
3. 身份证实名认证。
4. 商家营业资质认证。
5. 敏感数据加密存储。
6. 人工审核流程。

## 8. 第一版验收标准

## 8.1 用户验收

1. 用户可以注册。
2. 用户可以登录。
3. 用户可以退出登录。
4. 用户可以查看当前登录用户信息。
5. 用户可以修改昵称、头像和简介。

## 8.2 浏览验收

1. 首页可以展示帖子列表。
2. 帖子卡片包含图片、标题、作者、点赞数、评论数。
3. 用户可以进入帖子详情。
4. 用户可以通过关键词搜索帖子。

## 8.3 上传验收

1. 登录用户可以上传帖子图片。
2. 登录用户可以发布帖子。
3. 发布后的帖子可以在首页看到。
4. 用户可以编辑自己的帖子。
5. 用户可以删除自己的帖子。

## 8.4 评论验收

1. 用户可以查看帖子评论。
2. 登录用户可以发表评论。
3. 评论发布后帖子评论数增加。
4. 用户可以删除自己的评论。
5. 用户不能删除别人的评论。

## 8.5 点赞验收

1. 登录用户可以点赞帖子。
2. 登录用户可以取消点赞。
3. 同一用户不能重复点赞同一帖子。
4. 点赞数展示正确。

## 8.6 我的页面验收

1. 用户可以查看自己的资料。
2. 用户可以查看自己发布的帖子。
3. 用户可以查看自己发表的评论。
4. 用户可以退出登录。

## 9. 开发里程碑

| 里程碑 | 内容 | 目标 |
|---|---|---|
| M1 | 工程初始化、数据库、基础接口 | 项目可启动 |
| M2 | 注册登录、用户资料 | 用户体系可用 |
| M3 | 首页列表、帖子详情、搜索 | 用户可浏览内容 |
| M4 | 图片上传、帖子发布、编辑删除 | 用户可上传内容 |
| M5 | 评论、点赞 | 基础互动可用 |
| M6 | 我的页面、体验修正 | 第一版 MVP 可验收 |

## 10. 实施原则

1. 第一版优先保证核心链路可用，不提前引入复杂业务。
2. 数据模型保留后续扩展空间，但接口行为先保持简单。
3. 指纹、AI、信用币、悬赏、商家认证等功能先不阻塞第一版。
4. 文件上传先支持本地开发存储，后续再切换对象存储。
5. 权限控制先覆盖基础场景：未登录不可发布、只能编辑删除自己的内容。
6. 所有接口先保证移动端可直接使用，管理后台后续再补。
